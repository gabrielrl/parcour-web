namespace PRKR.Model {

  /** Enumeration of possible tile types. */
  export enum TileType {

    /** Normal floor tile (default). */
    Floor = 0,

    /** (Bottomless) hole. Watch your steps. */
    Hole = -1
  }
}